'use strict';

module.exports = function* installAlinode() {
  // requestOptions.distUrl = options.alinodeDistUrl;
  // requestOptions.version = yield version.getAlinodeVersion({
  //   version: options.alinodeVersion,
  //   distUrl: options.alinodeDistUrl,
  // });
  // if (requestOptions.version === versions.alinode) {
  //   return;
  // }
};
